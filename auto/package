#!/bin/sh

set -ex

export NODE_VERSION=10.14.2
export SHA256=e43de13bf7bee440a106a844c1bc3a2adb8829fd58b857702c8f1838fdd02a2a

DIR="$( cd "$( dirname "$0" )" && pwd )"

cd "${DIR}/.."

docker-compose build
docker-compose run --rm \
  -e NODE_VERSION -e SHA256 \
  package
