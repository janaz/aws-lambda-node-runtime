#!/bin/sh

set -ex

export NODE_VERSION=14.2.0
export SHA256=468cbd92271da8c0cacaa3fa432a73a332e398bade8ad7359a94aa8ab3cc3cca

DIR="$( cd "$( dirname "$0" )" && pwd )"

cd "${DIR}/.."

docker-compose build
docker-compose run --rm \
  -e NODE_VERSION -e SHA256 \
  package
